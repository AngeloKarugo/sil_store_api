apiVersion: v1
kind: Service
metadata:
    name: sil-store-api-service
spec:
    type: NodePort
    selector:
        app: sil-store-api
    ports:
        - protocol: TCP
          port: 80
          targetPort: 8010
          nodePort: 30000
